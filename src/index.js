import "./styles.css";
import "./main-ui.js";
import "./add-new-task.js";

//Make sure that when add new item is clicked, until it's closed, the 3dots icons in items-display should be disabled.
//Write logic to edit task object at open-task-page.js
//Write logic to edit items in items-display.js //Make sure only one item can be edited at a time.
//Listen to new task/new item submit buttons, when each inner button is on change, if all required fields are filled, enable the submit buttons.